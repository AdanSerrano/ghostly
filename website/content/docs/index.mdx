---
title: Introduction
description: Zero-config skeleton loaders for React. Wrap your component, done.
---

import { Cards, Card } from 'fumadocs-ui/components/card'
import { Callout } from 'fumadocs-ui/components/callout'

**Ghostly** transforms your existing components into skeleton loaders using pure CSS. No CLI, no build step, no code changes.

```tsx title="Just wrap your component"
import { Ghostly } from '@ghostly-ui/react'
import '@ghostly-ui/core/css'

<Ghostly loading={isLoading}>
  <ProductCard product={data} />
</Ghostly>
```

Your component **is** the skeleton. Ghostly uses CSS to hide text and images, replacing them with animated placeholder blocks — preserving the exact same layout.

## Why Ghostly?

Most skeleton libraries require you to **rewrite** your components, **run CLI tools**, or **maintain generated files**. Ghostly doesn't.

| | Traditional | Ghostly |
|---|---|---|
| Setup | CLI + config + build | `npm install` + CSS import |
| Code changes | Rewrite components | None |
| Maintenance | Regenerate on changes | Zero |
| Bundle size | 4-16KB + runtime JS | ~3KB, zero runtime JS |
| Suspense | Manual wiring | `<GhostlySuspense>` built-in |

## Quick Start

<Cards>
  <Card title="Installation" href="/docs/getting-started/installation">
    Install Ghostly in under 30 seconds
  </Card>
  <Card title="Basic Usage" href="/docs/getting-started/usage">
    Learn the basics of wrapping components
  </Card>
  <Card title="API Reference" href="/docs/reference/components">
    Full component, hook, and prop reference
  </Card>
  <Card title="Examples" href="/docs/guides/examples">
    Real-world patterns and integrations
  </Card>
  <Card title="CLI" href="/docs/getting-started/cli">
    Auto-generate loading.tsx, setup wizard, health check
  </Card>
</Cards>

## Features

- **Zero config** — Install, import CSS, wrap. No build step, no config files
- **CLI tool** — `ghostly init` to set up, `ghostly add loading` to auto-generate loading.tsx, `ghostly doctor` to diagnose
- **Zero code changes** — Your components stay exactly as they are
- **CSS-first** — All skeleton logic is pure CSS. Zero JavaScript overhead at runtime
- **GhostlySuspense** — Automatic skeleton with React Suspense. No loading state needed
- **Smooth transitions** — Fade-out when loading ends with the `smooth` prop
- **Color props** — Customize colors per-instance with `color` and `shine` props
- **Line control** — `data-ghostly-lines` for text, `data-ghostly-ratio` for images
- **Tailwind plugin** — Utility classes: `ghostly-radius-*`, `ghostly-speed-*`, `ghostly:` variant
- **MCP server** — AI assistants (Claude, Cursor, Copilot) get real documentation
- **Progressive** — Works with CSR, SSR, RSC, and Suspense
- **Accessible** — ARIA attributes, reduced motion, screen reader friendly
- **Tiny** — ~2KB CSS + ~1KB React (gzipped). Zero runtime dependencies in core

## Packages

| Package | Description | Size (gzip) |
|---------|-------------|-------------|
| `@ghostly-ui/cli` | CLI: init, add loading, doctor | — |
| `@ghostly-ui/core` | CSS engine + types + Tailwind plugin | ~2KB |
| `@ghostly-ui/react` | React components | ~1KB |
