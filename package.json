{
  "name": "ghostly-monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun run --filter website dev",
    "build": "bun run build:core && bun run build:react && bun run build:vue && bun run build:svelte",
    "build:core": "bun run --filter @ghostly-ui/core build",
    "build:react": "bun run --filter @ghostly-ui/react build",
    "build:vue": "bun run --filter @ghostly-ui/vue build",
    "build:svelte": "bun run --filter @ghostly-ui/svelte build",
    "lint": "eslint packages/*/src/**/*.{ts,tsx}",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "type-check": "tsc -b",
    "clean": "rm -rf packages/*/dist website/.next",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "prepublish": "bun run build"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@testing-library/vue": "^8.1.0",
    "@types/node": "^22.19.15",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitest/coverage-v8": "^4.1.0",
    "@vue/test-utils": "^2.4.6",
    "eslint": "^9.39.4",
    "jsdom": "^28.1.0",
    "prettier": "^3.8.1",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "tsup": "^8.5.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.33.1",
    "vitest": "^4.1.0"
  },
  "engines": {
    "node": ">=20",
    "bun": ">=1.2"
  }
}
